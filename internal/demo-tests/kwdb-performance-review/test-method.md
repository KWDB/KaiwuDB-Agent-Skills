# Test Method

1. run trigger tests and confirm the skill loads only when expected
2. run functional tests and verify the workflow order and MCP usage
3. run regression prompts after every meaningful change
4. confirm the output includes scope, severity-ranked issues, and summary
